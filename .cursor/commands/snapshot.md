# Snapshot Studio state

Pull the current Studio DataModel state through the bridge (or via the Roblox Studio MCP server if connected) and summarize what's there — instance tree, relevant properties, and anything that differs from the last snapshot. Use this to verify a change actually landed in Studio instead of guessing. Report the diff, not the entire tree.
